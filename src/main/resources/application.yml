spring:
    datasource:
        url: jdbc:mysql://0.0.0.0:3306
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            spring.jpa.hibernate.ddl-auto=update
    session:
        store-type: redis
